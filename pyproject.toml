[project]
name = "auto-dab"
version = "0.1.0"
description = "Autoresearch for antibody masked diffusion language models"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.4",
    "einops",
    "numpy",
    "pandas",
    "pyarrow",
]
